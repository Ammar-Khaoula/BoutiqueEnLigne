{% extends 'base.html.twig' %}

{% block body %}
<div class="container my-5">
    <div class="card">
    <div class="row g-0">
        <div class="col-md-4">
        <img src="/assets/images/{{ product.images }}" class="img-fluid rounded-start" alt="...">
        </div>
        <div class="col-md-8">
        <div class="card-body">
            <a href="{{ path('app_category', { slug : product.category.slug }) }}"> {{ product.category.name}} </a> <i>>{{ product.name }}</i>
            <h2 class="card-title nameProduct">{{ product.name }}</h2>
            <h5 class="d-block mb-3"> {{ product.pricewt }}€ </h5>
            <p class="card-text">{{ product.description|raw }}</p>
            <a href="{{ path('app_cart_add', { id : product.id }) }}" class="btn btn-success"> Ajouter au panier</a>
        </div>
        </div>
    </div>
    </div>
</div>
{% endblock %}
