{% extends 'base.html.twig' %}

{% block body %}
    <div class="container my-5 text-center">
        <img src="/assets/images/check.png" alt="commande confirmer">
        <h1>Merci pour votre commande</h1>
        <a href="{{ path('app_account_order', { 'id_order' : order.id }) }}" class="btn btn-success"> consulter ma commande</a>
    </div>
{% endblock %}
